# Minigrep - An I/O Project: Building a Command Line Program

Let's build a 'minigrep', a `grep` that receives a string to search for and a path to a file to search in, like so:

```Shell
cargo run -- searchstring example-filename.txt
```

